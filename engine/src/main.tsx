import React,{useEffect,useRef,useState} from 'react';
import {createRoot} from 'react-dom/client';
import {ArcRotateCamera,Color3,Color4,Engine,GizmoManager,HemisphericLight,Mesh,MeshBuilder,PointLight,Scene,SceneLoader,StandardMaterial,TransformNode,Vector3,VertexBuffer} from '@babylonjs/core';
import '@babylonjs/loaders/glTF';
import JSZip from 'jszip';
import type {MapLight,MapSkybox,Spawn} from '../../shared/maps';
import './style.css';
type Team='A'|'B';type SpawnMarker=Spawn&{id:string;team:Team};type Selected={kind:'reference'|'spawn'|'light';id:string};
const uid=(prefix:string)=>`${prefix}-${crypto.randomUUID().slice(0,8)}`;
const cleanId=(s:string)=>s.toLowerCase().trim().replace(/[^a-z0-9-]+/g,'-').replace(/^-+|-+$/g,'').slice(0,40);
const save=(blob:Blob,name:string)=>{const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);};
const skyColors:Record<Exclude<MapSkybox['preset'],'custom'>,{clear:Color4;ambient:Color3}>={'blue-day':{clear:new Color4(.28,.57,.86,1),ambient:new Color3(.85,.92,1)},overcast:{clear:new Color4(.38,.43,.47,1),ambient:new Color3(.72,.76,.78)},night:{clear:new Color4(.012,.022,.055,1),ambient:new Color3(.18,.25,.42)}};
function App(){
 const canvas=useRef<HTMLCanvasElement>(null),scene=useRef<Scene|undefined>(undefined),root=useRef<TransformNode|undefined>(undefined),meshes=useRef<Mesh[]>([]),source=useRef<ArrayBuffer|undefined>(undefined);
 const reference=useRef<Mesh|undefined>(undefined),gizmos=useRef<GizmoManager|undefined>(undefined),spawnNodes=useRef(new Map<string,TransformNode>()),lightNodes=useRef(new Map<string,TransformNode>()),ambient=useRef<HemisphericLight|undefined>(undefined);
 const [name,setName]=useState('New map'),[id,setId]=useState('new-map'),[scale,setScale]=useState(1),[offsetY,setOffsetY]=useState(0),[cursor,setCursor]=useState({x:0,y:0,z:0,yaw:0});
 const [spawns,setSpawns]=useState<SpawnMarker[]>([]),[lights,setLights]=useState<MapLight[]>([]),[selected,setSelected]=useState<Selected>({kind:'reference',id:'reference'});
 const [sky,setSky]=useState<MapSkybox>({preset:'blue-day'}),skyFile=useRef<ArrayBuffer|undefined>(undefined),[status,setStatus]=useState('Import a GLB to begin.'),[exporting,setExporting]=useState<{stage:string;progress:number}>();
 const selectedRef=useRef(selected);selectedRef.current=selected;
 const selectedNode=()=>{const choice=selectedRef.current;return choice.kind==='reference'?reference.current:choice.kind==='spawn'?spawnNodes.current.get(choice.id):lightNodes.current.get(choice.id);};
 const attach=()=>gizmos.current?.attachToNode(selectedNode()??null);
 const commitSelected=()=>{const node=selectedNode(),choice=selectedRef.current;if(!node)return;if(choice.kind==='reference')setCursor({x:+node.position.x.toFixed(3),y:+(node.position.y-.9).toFixed(3),z:+node.position.z.toFixed(3),yaw:+node.rotation.y.toFixed(3)});if(choice.kind==='spawn')setSpawns(v=>v.map(s=>s.id===choice.id?{...s,x:+node.position.x.toFixed(3),y:+node.position.y.toFixed(3),z:+node.position.z.toFixed(3),yaw:+node.rotation.y.toFixed(3)}:s));if(choice.kind==='light')setLights(v=>v.map(l=>l.id===choice.id?{...l,x:+node.position.x.toFixed(3),y:+node.position.y.toFixed(3),z:+node.position.z.toFixed(3)}:l));};
 useEffect(()=>{const engine=new Engine(canvas.current!,true),s=scene.current=new Scene(engine);s.clearColor=skyColors['blue-day'].clear;const camera=new ArcRotateCamera('camera',-Math.PI/2,1.05,18,new Vector3(0,1,0),s);camera.attachControl(canvas.current!,true);camera.wheelPrecision=35;camera.minZ=.03;ambient.current=new HemisphericLight('ambient',Vector3.Up(),s);ambient.current.intensity=.9;const ground=MeshBuilder.CreateGround('editor-grid',{width:100,height:100},s),gm=new StandardMaterial('grid',s);gm.diffuseColor=new Color3(.08,.1,.11);gm.wireframe=true;ground.material=gm;const capsule=reference.current=MeshBuilder.CreateCapsule('player-reference',{height:1.8,radius:.32},s),cm=new StandardMaterial('reference-material',s);cm.emissiveColor=new Color3(.1,.65,1);cm.alpha=.48;cm.wireframe=true;capsule.material=cm;capsule.position.y=.9;capsule.metadata={editor:{kind:'reference',id:'reference'}};const manager=gizmos.current=new GizmoManager(s);manager.positionGizmoEnabled=true;manager.rotationGizmoEnabled=true;manager.scaleGizmoEnabled=false;manager.boundingBoxGizmoEnabled=false;manager.usePointerToAttachGizmos=false;manager.attachToNode(capsule);s.onPointerDown=(_event,pick)=>{const entity=pick.pickedMesh?.metadata?.editor as Selected|undefined;if(entity)setSelected(entity);};const commit=()=>commitSelected();window.addEventListener('pointerup',commit);const resize=()=>engine.resize();window.addEventListener('resize',resize);engine.runRenderLoop(()=>s.render());return()=>{window.removeEventListener('pointerup',commit);window.removeEventListener('resize',resize);engine.dispose();};},[]);
 useEffect(()=>{root.current?.scaling.setAll(scale);if(root.current)root.current.position.y=offsetY;},[scale,offsetY]);
 useEffect(()=>{const n=reference.current;if(!n)return;n.position.set(cursor.x,cursor.y+.9,cursor.z);n.rotation.y=cursor.yaw;if(selected.kind==='reference')attach();},[cursor,selected]);
 useEffect(()=>{const s=scene.current;if(!s)return;for(const n of spawnNodes.current.values())n.dispose();spawnNodes.current.clear();for(const spawn of spawns){const n=new TransformNode(spawn.id,s),body=MeshBuilder.CreateCapsule(`${spawn.id}-body`,{height:1.8,radius:.32},s),mat=new StandardMaterial(`${spawn.id}-material`,s);mat.emissiveColor=spawn.team==='A'?new Color3(.1,.5,1):new Color3(1,.2,.15);mat.alpha=.58;body.material=mat;body.parent=n;body.position.y=.9;body.metadata={editor:{kind:'spawn',id:spawn.id}};n.position.set(spawn.x,spawn.y??0,spawn.z);n.rotation.y=spawn.yaw;spawnNodes.current.set(spawn.id,n);}attach();},[spawns,selected]);
 useEffect(()=>{const s=scene.current;if(!s)return;for(const n of lightNodes.current.values())n.dispose();lightNodes.current.clear();for(const light of lights){const n=new TransformNode(light.id,s),bulb=MeshBuilder.CreateSphere(`${light.id}-bulb`,{diameter:.22},s),mat=new StandardMaterial(`${light.id}-material`,s);mat.emissiveColor=Color3.FromHexString(light.color);bulb.material=mat;bulb.parent=n;bulb.metadata={editor:{kind:'light',id:light.id}};const lamp=new PointLight(`${light.id}-source`,Vector3.Zero(),s);lamp.parent=n;lamp.diffuse=Color3.FromHexString(light.color);lamp.intensity=light.intensity;lamp.range=light.range;n.position.set(light.x,light.y,light.z);lightNodes.current.set(light.id,n);}attach();},[lights,selected]);
 useEffect(()=>{const s=scene.current;if(!s)return;const colors=sky.preset==='custom'?skyColors['blue-day']:skyColors[sky.preset];s.clearColor=colors.clear;if(ambient.current){ambient.current.diffuse=colors.ambient;ambient.current.intensity=sky.preset==='night'?.35:.9;}},[sky]);
 async function load(file:File){if(!scene.current)return;root.current?.dispose();meshes.current=[];setSpawns([]);setLights([]);setSelected({kind:'reference',id:'reference'});source.current=await file.arrayBuffer();const mapRoot=root.current=new TransformNode('authored-map',scene.current);mapRoot.scaling.setAll(scale);mapRoot.position.y=offsetY;const result=await SceneLoader.ImportMeshAsync('','',file,scene.current);for(const node of [...result.transformNodes,...result.meshes])if(!node.parent)node.parent=mapRoot;meshes.current=result.meshes.filter((m):m is Mesh=>m instanceof Mesh&&m.getTotalVertices()>0);setName(file.name.replace(/\.glb$/i,'').replace(/[-_]+/g,' '));setId(cleanId(file.name.replace(/\.glb$/i,'')));setStatus(`Loaded ${meshes.current.length} meshes. Add an object, select it, then drag its axis gizmo.`);}
 function addSpawn(team:Team){if(spawns.filter(s=>s.team===team).length>=2)return setStatus(`Team ${team} already has two spawns.`);const marker:SpawnMarker={id:uid(`spawn-${team}`),team,...cursor};setSpawns(v=>[...v,marker]);setSelected({kind:'spawn',id:marker.id});}
 function addLight(){const light:MapLight={id:uid('light'),type:'point',x:cursor.x,y:cursor.y+2,z:cursor.z,color:'#fff1d6',intensity:2,range:8};setLights(v=>[...v,light]);setSelected({kind:'light',id:light.id});}
 function bake(){const triangles:number[][]=[];let minX=Infinity,maxX=-Infinity,minZ=Infinity,maxZ=-Infinity;for(const mesh of meshes.current){mesh.computeWorldMatrix(true);const positions=mesh.getVerticesData(VertexBuffer.PositionKind),indices=mesh.getIndices();if(!positions||!indices)continue;const world=mesh.getWorldMatrix();for(let i=0;i<indices.length;i+=3){const row:number[]=[];for(let j=0;j<3;j++){const at=indices[i+j]*3,p=Vector3.TransformCoordinates(new Vector3(positions[at],positions[at+1],positions[at+2]),world);minX=Math.min(minX,p.x);maxX=Math.max(maxX,p.x);minZ=Math.min(minZ,p.z);maxZ=Math.max(maxZ,p.z);row.push(+p.x.toFixed(5),+p.y.toFixed(5),+p.z.toFixed(5));}triangles.push(row);}}return {triangles,bounds:{minX,maxX,minZ,maxZ}};}
 async function exportMap(){try{if(!source.current)throw new Error('Import a GLB first.');if(spawns.filter(s=>s.team==='A').length<2||spawns.filter(s=>s.team==='B').length<2)throw new Error('Add two spawn points for each team.');if(sky.preset==='custom'&&!skyFile.current)throw new Error('Upload a .env skybox or choose a preset.');const mapId=cleanId(id);if(!mapId)throw new Error('Enter a valid folder ID.');setExporting({stage:'Baking collision geometry',progress:8});await new Promise(r=>setTimeout(r,30));const baked=bake();if(!baked.triangles.length)throw new Error('No collision triangles were found in this GLB.');setExporting({stage:`Writing ${baked.triangles.length.toLocaleString()} collision triangles`,progress:35});await new Promise(r=>setTimeout(r,30));const manifest={version:1,id:mapId,name:name.trim(),asset:`/maps/${mapId}/map.glb`,walls:[],triangles:baked.triangles,offsetY,scale,bounds:baked.bounds,spawns:{A:spawns.filter(s=>s.team==='A').map(({id:_,team:__,...s})=>s),B:spawns.filter(s=>s.team==='B').map(({id:_,team:__,...s})=>s)},lights,skybox:{preset:sky.preset,asset:sky.preset==='custom'?`/maps/${mapId}/skybox.env`:undefined}};const zip=new JSZip(),folder=zip.folder(mapId)!;folder.file('map.glb',source.current);folder.file('map.json',JSON.stringify(manifest));if(sky.preset==='custom')folder.file('skybox.env',skyFile.current!);folder.file('INSTALL.txt','Copy this entire folder into client/public/maps, then restart the Collateral server.');const blob=await zip.generateAsync({type:'blob',compression:'DEFLATE',compressionOptions:{level:6}},meta=>setExporting({stage:'Compressing map package',progress:35+meta.percent*.65}));save(blob,`${mapId}.zip`);setStatus(`Exported ${baked.triangles.length.toLocaleString()} collision triangles.`);setExporting({stage:'Export complete',progress:100});setTimeout(()=>setExporting(undefined),700);}catch(error){setStatus((error as Error).message);setExporting(undefined);}}
 const selectObject=(kind:Selected['kind'],objectId:string)=>setSelected({kind,id:objectId});
 return <div className="app"><header><b>C/</b><div><h1>MAP ENGINE</h1><small>PACKAGE FORMAT V1</small></div><button className="export" onClick={exportMap}>EXPORT MAP PACKAGE</button></header><aside><section><h2>1 · Source</h2><label>Map GLB<input type="file" accept=".glb,model/gltf-binary" onChange={e=>e.target.files?.[0]&&void load(e.target.files[0])}/></label><label>Display name<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Folder ID<input value={id} onChange={e=>setId(cleanId(e.target.value))}/></label><div className="row"><label>Scale<input type="number" min=".01" step=".05" value={scale} onChange={e=>setScale(+e.target.value)}/></label><label>Y offset<input type="number" step=".05" value={offsetY} onChange={e=>setOffsetY(+e.target.value)}/></label></div></section><section><h2>2 · Objects</h2><p>Select an object, then drag its red, green, or blue axis. Rotation handles set spawn facing.</p><button className={selected.kind==='reference'?'selected':''} onClick={()=>selectObject('reference','reference')}>PLAYER REFERENCE</button><div className="row"><button onClick={()=>addSpawn('A')}>ADD TEAM A SPAWN</button><button onClick={()=>addSpawn('B')}>ADD TEAM B SPAWN</button></div>{spawns.map(s=><div className={`object ${selected.id===s.id?'active':''}`} key={s.id}><button onClick={()=>selectObject('spawn',s.id)}>TEAM {s.team} · {spawns.filter(x=>x.team===s.team).indexOf(s)+1}<small>{s.x.toFixed(2)}, {Number(s.y??0).toFixed(2)}, {s.z.toFixed(2)}</small></button><button onClick={()=>setSpawns(v=>v.filter(x=>x.id!==s.id))}>×</button></div>)}<button onClick={addLight}>ADD POINT LIGHT</button>{lights.map((l,i)=><div className={`light ${selected.id===l.id?'active':''}`} key={l.id}><button onClick={()=>selectObject('light',l.id)}>{l.id}</button><input type="color" value={l.color} onChange={e=>setLights(v=>v.map((x,n)=>n===i?{...x,color:e.target.value}:x))}/><label>Power<input type="number" step=".1" value={l.intensity} onChange={e=>setLights(v=>v.map((x,n)=>n===i?{...x,intensity:+e.target.value}:x))}/></label><label>Range<input type="number" step=".5" value={l.range} onChange={e=>setLights(v=>v.map((x,n)=>n===i?{...x,range:+e.target.value}:x))}/></label><button onClick={()=>setLights(v=>v.filter((_,n)=>n!==i))}>×</button></div>)}</section><section><h2>3 · Skybox</h2><label>Environment<select value={sky.preset} onChange={e=>setSky({preset:e.target.value as MapSkybox['preset']})}><option value="blue-day">Blue Day</option><option value="overcast">Overcast</option><option value="night">Night</option><option value="custom">Custom Babylon .env</option></select></label>{sky.preset==='custom'&&<label>Skybox .env<input type="file" accept=".env" onChange={async e=>{const file=e.target.files?.[0];skyFile.current=file?await file.arrayBuffer():undefined;}}/></label>}</section><footer>{status}</footer></aside><main><canvas ref={canvas}/><div className="help">LEFT DRAG · ORBIT &nbsp; RIGHT DRAG · PAN &nbsp; WHEEL · ZOOM &nbsp; CLICK MARKER · SELECT</div></main>{exporting&&<div className="modal"><div><h2>EXPORTING MAP</h2><p>{exporting.stage}</p><progress max="100" value={exporting.progress}/><strong>{Math.round(exporting.progress)}%</strong></div></div>}</div>;
}
createRoot(document.getElementById('root')!).render(<App/>);
